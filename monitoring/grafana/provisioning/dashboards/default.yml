# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'safepath-dashboards'
    orgId: 1
    folder: 'CoT SafePath Filter'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards